import 'package:flutter/material.dart';

class NotificatioinScreen extends StatelessWidget {
  const NotificatioinScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("NotificatioinScreen")));
  }
}
